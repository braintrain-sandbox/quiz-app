# Database Connection
# Supabase Transaction Pooler (recommended for Prisma):
# DATABASE_URL="postgresql://postgres.xxxxx:password@aws-0-region.pooler.supabase.com:6543/postgres"

DATABASE_URL="postgresql://username:password@localhost:5432/quiz_app?schema=public"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"

# Generate secret with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"

# Google OAuth (optional - only needed for Google sign-in)
# Get from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Email Verification (Node SMTP)
# Configure with your SMTP provider (Gmail, Zoho Mail, Mailgun, etc.)
EMAIL_SERVER_HOST="smtp.your-provider.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_SECURE="false"
EMAIL_SERVER_USER="your-smtp-username"
EMAIL_SERVER_PASSWORD="your-smtp-password"
EMAIL_FROM="Quiz App <no-reply@yourdomain.com>"

# Razorpay Payment Gateway
RAZORPAY_KEY_ID="your-razorpay-key-id"
RAZORPAY_KEY_SECRET="your-razorpay-key-secret"
NEXT_PUBLIC_RAZORPAY_KEY_ID="your-razorpay-key-id"
# Course checkout price in INR
COURSE_PRICE_INR="1495"

# Zoho Invoice API
ZOHO_CLIENT_ID="your-zoho-client-id"
ZOHO_CLIENT_SECRET="your-zoho-client-secret"
ZOHO_REFRESH_TOKEN="your-zoho-refresh-token"
ZOHO_ORGANIZATION_ID="your-zoho-organization-id"
# Optional overrides (India defaults are used in code)
# ZOHO_OAUTH_TOKEN_URL="https://accounts.zoho.in/oauth/v2/token"
# ZOHO_INVOICE_API_BASE_URL="https://invoice.zoho.in/api/v3"

# Cashfree Payment Gateway (International users)
# Get credentials from: https://merchant.cashfree.com/merchants/pg
CASHFREE_APP_ID=
CASHFREE_SECRET_KEY=
# "sandbox" for testing, "production" for live payments
CASHFREE_ENV=sandbox
# Same value exposed to the browser for Cashfree JS SDK mode ("sandbox" | "production")
NEXT_PUBLIC_CASHFREE_ENV=sandbox